function ConvertFrom-StringParameter {
    param (
        [hashtable] $Parameter,
        [string] $DestinationFunctionName
    )

    # Use the function name to determine the function parametrs and compare expected types with thypes in the $Parameters hashtable

    # return a (potentially) modified 
}